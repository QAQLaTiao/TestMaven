<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- MyBatis 设置 -->
	<settings>
		<!-- 禁用全局延迟加载，所有关联对象都会即时加载 -->
		<setting name="lazyLoadingEnabled" value="false" />
		<!-- 启用下划线与驼峰式命名规则的映射 -->
		<setting name="mapUnderscoreToCamelCase" value="true" />
        <!-- 打印查询语句 -->
<!-- 		<setting name="logImpl" value="STDOUT_LOGGING" />	 -->
	</settings>
        
	<!-- Mappers 映射 -->
	<!-- 
	Spring 配置文件已为 MyBatis 的 SqlSessionFactory 指定了 mapperLocations,
	请将Mapper 文件放置在 conf/mapper/*Mapper.xml 中，Spring 将自动扫描 (无需手动添加) 
	-->
	
	<!-- <mappers> <mapper resource="conf/mapper/WebInfoMapper.xml" /> </mappers> -->
</configuration>